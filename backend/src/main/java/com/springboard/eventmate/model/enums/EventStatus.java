package com.springboard.eventmate.model.enums;

public enum EventStatus {

    ACTIVE,   // Event is open for booking
    FULL,     // Capacity reached, no more bookings
    CLOSED    // Manually closed or expired
}
